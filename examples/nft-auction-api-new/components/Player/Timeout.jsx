import React from 'react';

const Timeout = ({outcome}) => {
    return(
      <div className="Game">
        There's a been a timeout. Someone took too long..
      </div>
    );
  }

  export default Timeout 